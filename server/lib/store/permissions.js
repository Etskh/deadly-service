'use strict';

const auth = require('../auth');

/*
const permissionFixtures = {
  'store': [
    'every',
    'create',
    'delete',
    'manage',
  ],
};


module.exports = auth.utils.parseAndLoadPermissions(permissionFixtures, {
  'administrator': [
    'pos.store.every',
    'pos.store.create',
    'pos.store.manage',
  ],
});
*/
