'use strict';

const util = require('./util');

/*
const authPermissionFixtures = {
  'auth': {
    'user': [
      'every',
      'create',
      'delete',
      'manage',
    ],
  },
};

module.exports = util.parseAndLoadPermissions(authPermissionFixtures, {
  'administrator': [
    'pos.auth.user.every',
    'pos.auth.user.create',
    'pos.auth.user.manage',
  ],
});
*/
